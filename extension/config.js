// The only line you need to change when deploying NeuroDesk somewhere
// other than localhost. Also add the deployed origin to `host_permissions`
// in manifest.json, or the browser will silently block the fetch calls.
export const APP_URL = "https://neuro-desk-ai-one.vercel.app";
