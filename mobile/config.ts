// The one line to change when pointing this app at something other than a
// local dev server. Same pattern as extension/config.js — every "shell"
// around the NeuroDesk web app (Chrome extension, desktop, mobile) reads
// from one small config file instead of having the URL scattered around.
export const APP_URL = "https://neuro-desk-ai-one.vercel.app";// 10.0.2.2 = host machine, from the Android emulator
